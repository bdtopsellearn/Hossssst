# ═══════════════════════════════════════════════════════════════
#   BD TopSell Bot — Python Dependencies
#   Install: pip install -r requirements.txt
# ═══════════════════════════════════════════════════════════════

# Telegram Bot Framework
python-telegram-bot==21.6

# Firebase Admin SDK (Realtime DB + Firestore + Auth)
firebase-admin==6.5.0

# Google Cloud Firestore (firebase-admin এর সাথে আসে, আলাদা দরকার নেই)
# কিন্তু VPS-এ কখনো কখনো আলাদা লাগে তাই রাখা হয়েছে
google-cloud-firestore==2.16.0

# HTTP requests (firebase-admin এর dependency)
requests==2.31.0

# Async support
httpx==0.27.0
